// Canonical definition lives with its producer (`computeAbilityCoverageSummary`).
// Re-exported here so the compare components keep a stable import path without
// hand-syncing a duplicate shape.
export type { AbilityCoverageSummary } from "../../optimizer/abilityCoverage";
